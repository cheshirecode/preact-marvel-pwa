version: "2"
options:
  verbose: true
syncs:
  # marvellicious-sync:
  #   sync_args: ['-perms=0']
  #   sync_strategy: 'unison'
  #   sync_host_ip: '127.0.0.1'
  #   sync_excludes: ['.gitignore','.git/*', '.*/node_modules/']
  #   src: './marvellicious'
  #   watch_excludes: ['.*/.git', '.*/node_modules/', '.gitignore']
  preact-pwa-sync:
    sync_args: ['-perms=0']
    sync_strategy: 'unison'
    sync_host_ip: '127.0.0.1'
    sync_excludes: ['.gitignore' ,'.git/*', '.*/node_modules/', 'docker-*.yml']
    src: '.'
    watch_excludes: ['.*/.git', '.*/node_modules/', '.gitignore', 'docker-*.yml']